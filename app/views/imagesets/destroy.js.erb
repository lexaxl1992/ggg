var imgset = document.getElementById('<%= j "imgset_"+@imagesetId %>')
imgset.parentNode.removeChild(imgset);
if (document.getElementById('<%= j "for_imgset_"+@imagesetId %>') != null) {
    document.getElementById('<%= j "for_imgset_"+@imagesetId %>').remove();
}