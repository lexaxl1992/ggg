var dialog = document.createElement('dialog')
dialog.className = "mdl-dialog adminDialog"
dialog.id = '<%= j "d_"+Digest::CRC32.hexdigest(Random.rand(1..100).to_s) %>'
dialog.innerHTML = "<%= j render 'edit'%>"
document.body.appendChild(dialog)
dialogPolyfill.registerDialog(dialog)
componentHandler.upgradeDom()
//init_fileInput()
dialog.showModal()
